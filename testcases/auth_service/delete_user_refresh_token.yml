- config:
    name: 删除用户某端或全端refresh_token
    base_url: ${ENV(auth_service_host)}

- test:
    name: user_id存在-type为USER-without_client_id不传递
    request:
        method: DELETE
        url: /tokens/users/${source_user_id()}?type=USER
    validate:
        - eq: [status_code, 200]