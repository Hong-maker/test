- config:
    name: 解绑微信
    base_url: ${ENV(tiger_api_host)}
    verify: False

- test:
    name: 用户未绑定微信
    api: api/account_api/v3_for_web/unbind_wechat.yml
    variables:
        - token: ${source_user_login_token()}
    validate:
        - eq: [status_code, 204]